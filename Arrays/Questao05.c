#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <math.h>
#include <time.h>


//Implemente um algoritmo que leia 5 números e os armazene em um vetor. Em seguida, escreva os
//números ímpares armazenados nas posições de índice ímpar do vetor

/*
int main()
{
    int numeros [5];

    printf("Informe os Elementos do VETOR:\n");
        for(int i = 0; i < 5; i++)
        {
            printf("Valor do Indice [%i]:", i);
            scanf("%i", &numeros[i]);
        }

    printf("\nElementos Impares Armazenados Nos Indices Impares do VETOR:\n");
        for(int i = 1; i < 5; i += 2)
        {
            if (numeros[i] % 2 != 0)
            {
                printf("%i ", numeros[i]);
            }
        }
    return 0;
}
//*/
