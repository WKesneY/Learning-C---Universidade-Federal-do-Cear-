#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <math.h>
#include <time.h>

//Implemente um algoritmo que leia 5 números e os armazene em um vetor. Mostre o maior elemento
//do vetor


/*
int main()
{
    int numeros[5];
    int maiorValor = -99999;

    printf("Informe os Elementos do VETOR:\n");
        for(int i = 0; i < 5; i++)
        {
            printf("Valor do Indice [%i]:", i);
            scanf("%i", &numeros[i]);
        }

    printf("O Maior Valor do VETOR é: ");
        for(int i = 0; i < 5; i++)
        {
            if (numeros[i] > maiorValor)
            {
                maiorValor = numeros[i];
            }
        }

    printf("%i", maiorValor);

    return 0;
}

//*/
